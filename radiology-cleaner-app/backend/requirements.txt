# requirements.txt

# Web server
Flask==3.0.3
Flask-CORS==4.0.1
gunicorn==22.0.0

# Core data science libraries
scikit-learn==1.5.0
pandas==2.2.2
joblib==1.4.2
numpy==1.26.4

# Other core dependencies
typing-extensions==4.12.2
python-dateutil==2.9.0.post0
psutil==5.9.8

# NOTE: The entire SpaCy/SciSpacy/MedSpacy stack is now installed
# directly in the build.sh script to ensure correct order and
# avoid compilation errors on the build server.
