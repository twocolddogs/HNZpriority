# -- Web Server --
Flask==3.0.3
Flask-CORS==4.0.1
gunicorn==22.0.0

# -- Core ML/NLP Libraries --
# NOTE: scikit-learn version pinned to avoid potential conflicts with older scispaCy.
scikit-learn==1.5.0
pandas==2.2.2
joblib==1.4.2
numpy==1.26.4

# -- SpaCy and MedspaCy NLP Pipeline Dependencies --
# Pinning spacy to a version compatible with scispacy 0.5.3
spacy==3.6.1

# IMPORTANT: Installing scispacy[spacy] to avoid the broken nmslib dependency
scispacy[spacy]==0.5.3
medspacy==1.3.1

# -- Vector Similarity Library --
# Using hnswlib which has reliable pre-built binaries
hnswlib==0.8.0

# -- Other Core Dependencies --
typing-extensions==4.12.2
python-dateutil==2.9.0.post0
psutil==5.9.8