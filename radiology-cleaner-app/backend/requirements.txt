# requirements.txt

# Web server
Flask==3.0.3
Flask-CORS==4.0.1
gunicorn==22.0.0

# Core ML/NLP libraries
scikit-learn==1.5.0
pandas==2.2.2
joblib==1.4.2
numpy==1.26.4

# --- SpaCy and MedspaCy NLP Pipeline Dependencies ---
# We are letting pip resolve the latest compatible versions of these libraries
# to avoid version conflicts in the deployment environment.
spacy
scispacy
medspacy

# Other core dependencies
typing-extensions==4.12.2
python-dateutil==2.9.0.post0
psutil==5.9.8

# NOTE: The ScispaCy model itself is not listed here.
# It should be installed separately via your build script.
